const input=document.getElementById('input'), count=document.getElementById('count'), lookup=document.getElementById('lookup'), results=document.getElementById('results');
input.addEventListener('input',()=>{if(input.value.length>500)input.value=input.value.slice(0,500);count.textContent=`${input.value.length} / 500`});
document.querySelectorAll('[data-example]').forEach(b=>b.onclick=()=>{input.value=b.dataset.example;count.textContent=`${input.value.length} / 500`;doLookup()});
lookup.onclick=doLookup; input.addEventListener('keydown',e=>{if((e.ctrlKey||e.metaKey)&&e.key==='Enter')doLookup()});
function esc(s){return s.replace(/[&<>\'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function doLookup(){const q=input.value.trim();if(!q)return;const low=q.toLowerCase();const found=PHRASES.filter(x=>low.includes(x.p.toLowerCase())).sort((a,b)=>b.p.length-a.p.length);results.classList.remove('hidden');if(!found.length){results.innerHTML=`<div class="result-head"><h2>Plainly</h2><span class="found">No exact match</span></div><div class="noresult"><strong>We don’t have that phrase yet.</strong><p>Try another expression from the examples, or add this phrase to <code>phrases.js</code>.</p></div>`;return}results.innerHTML=`<div class="result-head"><h2>Here’s what it means</h2><span class="found">${found.length} expression${found.length===1?'':'s'} found</span></div>`+found.map(x=>`<article class="card"><div class="phrase">${esc(x.p)}</div><span class="type">${esc(x.type)}</span><div class="grid"><div class="field"><label>Actual meaning</label><p>${esc(x.meaning)}</p></div><div class="field"><label>What it implies</label><p>${esc(x.implies)}</p></div><div class="field"><label>Tone</label><p>${esc(x.tone)}</p></div><div class="field"><label>Literal meaning</label><p>${esc(x.literal)}</p></div></div><div class="example">${esc(x.example)}</div><div class="field" style="margin-top:18px"><label>Similar</label><p>${x.similar.map(esc).join(' · ')}</p></div></article>`).join('');results.scrollIntoView({behavior:'smooth',block:'start'})}
